# Results

Each `python run_hfss.py --analyze-all` run writes a timestamped folder here (`dashboard.html`, CSVs, metadata).

These generated folders are not committed. Re-run the pipeline to reproduce them.
